#include <stdio.h>

#define INF 999999

struct Edge {
    int u, v, w;
};

int main() {
    int V, E, src;

    scanf("%d", &V);
    scanf("%d", &E);

    struct Edge edges[E];

    for (int i = 0; i < E; i++) {
        scanf("%d %d %d", &edges[i].u, &edges[i].v, &edges[i].w);
    }

    scanf("%d", &src);

    int dist[V + 1];
    int parent[V + 1];

    // Initialize
    for (int i = 1; i <= V; i++) {
        dist[i] = INF;
        parent[i] = -1;
    }

    dist[src] = 0;

    // Relax all edges V-1 times
    for (int i = 1; i <= V - 1; i++) {
        int changed = 0;
}
